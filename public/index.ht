<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>System Notice</title>
    <style>
        @keyframes matrixRain {
            0% { background-position: 0% 0%; }
            100% { background-position: 0% 100%; }
        }

        @keyframes glitch {
            0% { text-shadow: 3px 0 #00ff00, -3px 0 #ff0000; }
            25% { text-shadow: -3px 0 #00ff00, 3px 0 #ff0000; }
            50% { text-shadow: 3px 0 #00ff00, -3px 0 #ff0000; }
            75% { text-shadow: -3px 0 #00ff00, 3px 0 #ff0000; }
            100% { text-shadow: 3px 0 #00ff00, -3px 0 #ff0000; }
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        body {
            margin: 0;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: #000;
            font-family: 'Courier New', monospace;
            color: #0f0;
            text-align: center;
            background-image: linear-gradient(0deg, rgba(0, 255, 0, 0.1) 1px, transparent 1px);
            background-size: 100% 2px;
            animation: matrixRain 20s linear infinite;
            position: relative;
        }

        .container {
            padding: 3rem;
            border-radius: 10px;
            background: rgba(0, 0, 0, 0.9);
            border: 2px solid #0f0;
            max-width: 600px;
            margin: 20px;
            position: relative;
            box-shadow: 0 0 20px rgba(0, 255, 0, 0.3);
        }

        .hacker-emoji {
            font-size: 8rem;
            margin-bottom: 2rem;
            animation: pulse 2s ease-in-out infinite;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            text-transform: uppercase;
            color: #0f0;
            animation: glitch 3s infinite;
        }

        p {
            font-size: 1.1rem;
            line-height: 1.6;
            margin-bottom: 2rem;
            color: #0f0;
            text-shadow: 0 0 5px #0f0;
        }

        .btn {
            display: inline-block;
            padding: 1rem 2rem;
            font-size: 1.1rem;
            text-decoration: none;
            color: #000;
            background: #0f0;
            border-radius: 5px;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            font-weight: bold;
            font-family: 'Courier New', monospace;
            text-transform: uppercase;
            box-shadow: 0 0 15px rgba(0, 255, 0, 0.5);
        }

        .btn:hover {
            background: #fff;
            box-shadow: 0 0 25px rgba(0, 255, 0, 0.8);
            transform: translateY(-3px);
        }

        .logo {
            font-size: 1.5rem;
            font-weight: bold;
            margin-top: 2rem;
            color: #0f0;
            text-shadow: 0 0 10px #0f0;
            letter-spacing: 3px;
        }

        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            padding: 1rem;
            background: rgba(0, 0, 0, 0.9);
            border-top: 1px solid #0f0;
            text-align: center;
            font-size: 0.9rem;
            color: #0f0;
            text-shadow: 0 0 5px #0f0;
            z-index: 1000;
        }

        .copyright {
            margin: 0;
            padding: 0;
            animation: glitch 5s infinite;
        }

        @media (max-width: 480px) {
            .container {
                padding: 1.5rem;
            }
            .hacker-emoji {
                font-size: 6rem;
            }
            h1 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="hacker-emoji">
            👨‍💻
        </div>
        <h1>System Access Restricted</h1>
        <p>[ NOTICE ]</p>
        <p>Due to copyright protocol violations, system access has been temporarily restricted. Our security team is implementing necessary patches.</p>
        <p>Join the secure channel below for status updates and restoration timeline.</p>
        <a href="https://chat.whatsapp.com/Jx7300IL1iVIaDUolVULBj" class="btn">> Join Secure Channel_</a>
        <p>Your cooperation in this matter is appreciated.</p>
        <div class="logo">TEAM MRLIT</div>
    </div>
    <footer class="footer">
        <p class="copyright">© 2024 Andy MRLIT. All rights reserved.</p>
    </footer>
</body>
</html>
